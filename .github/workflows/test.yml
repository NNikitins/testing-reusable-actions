name: Call a reusable workflow

on:
  pull_request:
    branches:
      - main

jobs:
  call-workflow:
    uses: NNikitins/reusable-github-actions/.github/actions/reusable-workflow.yml@main
    with:
      username: Nikita